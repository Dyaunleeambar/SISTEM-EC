# Configuración de autenticación y seguridad

# Clave JWT (cámbiala en producción)
JWT_SECRET=super-secret-dev-key-change-me

# Duración del token de acceso (login). 365 días
JWT_EXPIRES_IN=365d

# Duración del refresh token (opcional, extiende si usas refresh)
JWT_REFRESH_EXPIRES_IN=400d

# Días para expirar contraseña (evita el 401 por caducidad)
PASSWORD_EXPIRY_DAYS=365

# Coste de bcrypt
BCRYPT_ROUNDS=12

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Puerto del backend
PORT=3001
# Configuración del Servidor
PORT=3001
NODE_ENV=development

# Configuración de Base de Datos
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Cuba123456
DB_NAME=colaboradores_db

# Configuración JWT
JWT_SECRET=tu_super_secreto_jwt_muy_seguro_2024
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# Configuración de Seguridad
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Configuración de Contraseñas
PASSWORD_EXPIRY_DAYS=30
MIN_PASSWORD_LENGTH=8 
